name: First workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: print uname
              run: uname -a
    seconf_job:
        runs-on: ubuntu-latest
        steps:
            - name: print bye
              run: |
                echo "Byee"
                exit 0
            - name: print goodbyee
              run: echo "goodbyee"

